<UserControl x:Class="innove.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="G.E.R.A.R.D"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <UniformGrid Grid.Row="1" Columns="2" Rows="2" Margin="0,0,0,20">
            <Button Content="Traduire"  Margin="8" FontSize="16" Click="TranslateButton_Click"/>
            <Button Content="Résumer"   Margin="8" FontSize="16" Click="SummaryButton_Click"/>
            <Button Content="Réécrire"  Margin="8" FontSize="16" Click="RewriteButton_Click"/>
            <Button Content="Répondre"  Margin="8" FontSize="16" Click="AnswerButton_Click"/>
        </UniformGrid>

        <TextBlock Grid.Row="2" Text="Prompt personnalisé :" Margin="0,0,0,5"/>

        <DockPanel Grid.Row="3">
            <Button Content="Envoyer"
                    DockPanel.Dock="Right"
                    Width="100"
                    Margin="5,0,0,0"
                    Click="CustomSendButton_Click"/>
            <TextBox x:Name="CustomPromptTextBox" Height="35" VerticalContentAlignment="Center"/>
        </DockPanel>
    </Grid>
</UserControl>
